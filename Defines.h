#pragma once

const int SIZE_SCREEN_X = 640;
const int SIZE_SCREEN_Y = 480;

const float SIZE_OF_GROUND_X = 100.0f;
const float SIZE_OF_GROUND_Z = 100.0f;

const float CAMERA_SPEED_ROTATION = 0.05f;
const float CAMERA_SPEED_FORWARD = 5.0f;
const float CAMERA_SPEED_HORIZONTAL = 4.0f;

const D3DXVECTOR3 BUILDING_ROTATION(0.0f, 0.0f, 0.0f);
const D3DXVECTOR3 BUILDING_SCALE(3.0f, 3.0f, 3.0f);
const D3DXVECTOR4 BUILDING_COLOR(0.05f, 0.05f, 0.24f, 1.0f);
// const D3DXVECTOR4 BUILDING_COLOR(0.55f, 0.35f, 0.17f, 1.0f);

const D3DXVECTOR3 BIRD_ROTATION(0.0f, 0.0f, 0.0f);
const D3DXVECTOR3 BIRD_SCALE(1.0f, 1.0f, 1.0f);
const D3DXVECTOR4 BIRD_COLOR(0.93f, 0.93f, 0.0f, 1.0f);

const float MIN_BIRD_SPEED = 3.0f;
const float MAX_BIRD_SPEED = 5.0f;
const float INITIAL_BIRD_SPEED = 4.0f;
const float AMOUNT_OF_BIRDS_SPEED = 40;

const int MAX_AMOUNT_OF_RED_BALLS = 5;

const D3DXVECTOR3 TERRAIN_POSITION(0.0f, -1.0f, 0.0f);
const D3DXVECTOR3 TERRAIN_ROTATION(0.0f, 0.0f, 0.0f);
const D3DXVECTOR3 TERRAIN_SCALE(SIZE_OF_GROUND_X, 0.1f, SIZE_OF_GROUND_Z);
const D3DXVECTOR4 TERRAIN_COLOR(0.1f, 0.9f, 0.1f, 1.0f);

const int AMOUNT_OF_FLOCKS = 2;
const int AMOUNT_OF_BIRDS_IN_FLOCK = 3;